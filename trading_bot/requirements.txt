# SafeTrendBot V5 — Requirements
# =================================

# Core trading
MetaTrader5>=5.0.45

# UI (tkinter is included in Python stdlib for Windows)
# On Linux: sudo apt-get install python3-tk
# On macOS: included with python3

# Build tools (pour le Builder)
pyinstaller>=5.0
pyarmor>=7.0
cython>=3.0

# Optional: pour autres brokers
# python-xtb-api>=1.0.0
# crypto-payment>=1.0.0

# Utils
requests>=2.28
numpy>=1.24.0
pandas>=2.0.0