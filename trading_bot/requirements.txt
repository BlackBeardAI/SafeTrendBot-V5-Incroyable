# SafeTrendBot V5 — Requirements
# =================================

# Core trading
MetaTrader5>=5.0.45

# WebSocket (pour brokers)
websocket-client>=1.6.0

# HTTP requests (pour API brokers)
requests>=2.28.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Build tools (pour le Builder)
pyinstaller>=5.0
pyarmor>=7.0
cython>=3.0

# Flask (pour serveur d'activation)
flask>=3.0.0
flask-cors>=4.0.0

# Optional: pour GUI moderne
# PyQt6>=6.0  (install separately)
# PyQt5>=5.0  (install separately)